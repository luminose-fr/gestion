# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Dependency directories
node_modules/

# Build outputs
dist/

# Environment variables and virtual environments
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.venv
venv/
ENV/

# System files
.DS_Store
Thumbs.db
# Généré par tools/import-notion (se régénère depuis fixtures/)
tools/import-notion/import.sql

# Sources des maquettes (/design) : le canevas publié en est la copie durable,
# et le fichier assemblé pèse plus de 2 Mo.
.design/

# Règles de voix : engendrées depuis packages/corpus/content/voix/regles-de-voix.md
# par le même `embarquer`. La fiche est la source ; ce fichier en est la copie.
packages/editorial/src/voice.ts

# Corpus embarqué dans le Worker : régénéré depuis packages/corpus/content
# par `npm run embarquer -w packages/corpus`, avant chaque dev et chaque
# déploiement. Le commiter ferait exister deux copies du corpus.
workers/api/src/genere/

# Exports de Claude Design — matériau de travail, jamais versionné
_design/
